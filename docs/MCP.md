# Binance MCP server

The full MCP server documentation moved to [`docs/MCP-SERVER.md`](MCP-SERVER.md).

Use:

```sh
npm run mcp
```

for stdio clients, or:

```sh
npm run mcp:http
```

for the lightweight HTTP health endpoint.
